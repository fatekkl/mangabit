<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/manga_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/manga_title"
        app:layout_constraintEnd_toStartOf="@id/manga_title"
        app:layout_constraintBottom_toBottomOf="parent"
        android:contentDescription="Mangá logo"
        android:src="@mipmap/ic_launcher"
        />

    <TextView
        android:id="@+id/manga_title"
        android:paddingStart="10dp"
        android:paddingTop="5dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Manga Titulo"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="20sp"
        app:layout_constraintStart_toEndOf="@+id/manga_img"
        app:layout_constraintBottom_toTopOf="@+id/manga_desc"
        />

    <TextView
        android:id="@+id/manga_desc"
        android:paddingStart="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@+id/manga_title"
        android:text="Type"
        android:textSize="8sp"
        app:layout_constraintTop_toBottomOf="@+id/manga_title" />


</androidx.constraintlayout.widget.ConstraintLayout>